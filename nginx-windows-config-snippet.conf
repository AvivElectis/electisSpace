# =====================================================================
# electisSpace - Windows nginx Configuration
# =====================================================================
#
# The full production nginx virtual host config is now maintained at:
#
#   nginx/solum_co_il.conf
#
# Copy that file to C:\nginx\conf\vhosts\solum_co_il.conf on the
# Windows Server, then test and reload:
#
#   nginx.exe -t
#   nginx.exe -s reload
#
# =====================================================================
