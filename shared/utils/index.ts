/**
 * @electis/shared — Cross-app utility functions
 *
 * Shared between electisSpace (admin) and electisCompass (employee app).
 * Import via: import { ... } from '@electis/shared/utils';
 */

// Utilities will be added as Compass features are implemented
