/**
 * @electis/shared — Cross-app constants
 *
 * Shared between electisSpace (admin) and electisCompass (employee app).
 * Import via: import { ... } from '@electis/shared/constants';
 */

// Constants will be added as Compass features are implemented
